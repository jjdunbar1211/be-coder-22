extends layout.pug

block content
    div(class='container')
        h2(class='text-center mb-4') Vista de productos
        if productExists
            table(class='table table-info')
                thead
                    tr
                        th(scope='col') Producto
                        th(scope='col') Precio
                        th(scope='col') Foto
                tbody
                each product in products
                    tr
                        td #{product.title}
                        td #{product.price}
                        td
                            img(style='width: 50px; height: auto' src=product.image alt='products')
        else
            div(class='alert alert-info' role='alert') No hay productos disponibles, agrega uno haciendo click 
                a(href='/api/products/agregar' class='alert-link') aqui
        a(href='/api/products' class='btn btn-dark') Volver al inicio